import {Sprite, Texture} from "pixi.js/dist/pixi";
import slotImage from "../../static/images/slot.png";

class Slot extends Sprite {
    constructor() {
        // super(Texture.fromImage(slotImage));
    }
}

export default Slot;